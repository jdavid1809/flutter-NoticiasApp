import 'package:flutter/material.dart';

class Tabs1Screen extends StatelessWidget {
  const Tabs1Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Tabs1Screen'),
      ),
    );
  }
}
